# SemiRisk 项目设计文档

## 1. 项目定位

SemiRisk 是面向半导体产业链的 AI 风险智能平台，覆盖数据采集、风险识别、AI 推理、预警派发、GIS 监控、企业画像、RAG 知识检索和报告生成闭环。

## 2. 技术栈

- 后端：Java 21、Spring Boot 3.5、Spring MVC、Validation、Actuator
- 构建：Maven Wrapper
- 前端：HTML、Tailwind CDN、ECharts、Iconify、原生 JavaScript
- 状态：演示环境使用内存状态；MySQL、Redis 等中间件配置默认指向 `192.168.101.128`
- 部署：可直接 `./mvnw spring-boot:run`，中间件可使用 `compose/middleware.yml`

## 3. 工程结构

```text
SemiRisk/
├── pom.xml
├── mvnw
├── src/main/java/com/semirisk
│   ├── SemiRiskApplication.java
│   ├── api
│   │   ├── ApiResponse.java
│   │   └── SemiRiskController.java
│   ├── config
│   │   └── WebConfig.java
│   └── service
│       └── SemiRiskStore.java
├── src/main/resources/static
│   ├── index.html
│   ├── dashboard.html
│   ├── data-upload.html
│   ├── risk-analysis.html
│   ├── risk-detail.html
│   ├── report-generation.html
│   ├── alert-center.html
│   ├── gis-map.html
│   ├── enterprise-profile.html
│   ├── knowledge-base.html
│   ├── system-management.html
│   └── semirisk-api.js
├── compose/
├── script/
└── doc/
```

## 4. 架构设计

系统当前采用可运行单体架构，按 API 模块模拟微服务边界。后续可拆分为：

- `semirisk-auth-service`：登录、密码重置、防爆破
- `semirisk-data-service`：上传、ETL、SSE 清洗日志
- `semirisk-risk-service`：风险评分、详情、传播路径、SOP
- `semirisk-alert-service`：告警过滤、忽略、批量处理
- `semirisk-report-service`：异步报告任务、文件导出
- `semirisk-rag-service`：知识检索、文档预览
- `semirisk-system-service`：用户、RBAC、模型、Agent、数据源

## 5. 中间件设计

所有中间件统一使用虚拟机 IP `192.168.101.128`：

- MySQL：业务数据持久化
- Redis：登录失败计数、Token、验证码、短期任务状态
- Elasticsearch：RAG Hybrid Search
- MinIO：上传文件、PDF、报告文件存储
- RabbitMQ：ETL、报告生成和 Agent 异步任务
- Nacos：后续微服务注册配置中心

当前版本为保证本机可立即运行，`SemiRiskStore` 使用内存态承载业务数据；`application.properties` 已预留 MySQL、Redis 连接配置。

## 6. 关键业务流

### 6.1 登录与防爆破

用户提交账号、密码、验证码标识。后端在 5 分钟窗口内统计失败次数，达到 5 次锁定 30 分钟。登录成功后写入 Session 并返回前端 Token。

### 6.2 数据上传清洗

前端支持点击、拖拽文件上传。后端限制 50MB，创建上传任务；用户点击解析后任务进入“导入成功”状态。SSE 接口实时推送 AI 清洗日志。

### 6.3 告警处理

预警中心从 `/api/alerts` 拉取告警，支持关键词、等级、状态组合筛选。忽略告警会调用后端更新状态，并在前端淡出行项。批量处理会批量流转为“处理中”。

### 6.4 报告生成

用户选择模板、语言、格式后创建异步报告任务。前端轮询任务进度，完成后下载报告文本文件。后续可替换为 PDF、Word、PPT 渲染服务。

### 6.5 系统管理

系统管理页支持用户新增、启停、模型 Ping、Agent 触发、数据源重连等交互，均绑定后端 API。

## 7. Git 分支规范

- `main`：稳定版本
- `develop`：集成分支
- `feature/maven-bootstrap`：Maven 与 Spring Boot 初始化
- `feature/static-pages`：目标页面复用与前端交互
- `feature/core-api`：业务 API 与内存状态服务
- `feature/docs`：文档、部署、数据库脚本

Commit 使用 `type(scope): subject`，例如 `feat(api): add alert workflow endpoints`。

## 8. 启动与验证

```bash
./mvnw test
./mvnw spring-boot:run
```

访问 `http://localhost:8080/index.html`，默认账号 `admin/password`。

