# SemiRisk 项目设计文档

## 1. 项目定位

SemiRisk 是面向半导体产业链的 AI 风险智能平台，覆盖数据采集、风险识别、AI 推理、预警派发、GIS 监控、企业画像、RAG 知识检索和报告生成闭环。

## 2. 技术栈

- 后端：Java 21、Spring Boot 3.5、Spring MVC、Validation、Actuator、Maven 多模块
- 前端：Vue 3、Vite、原生 CSS、Fetch API
- 构建：Maven Wrapper
- 状态：公开源爬虫数据驱动风险页面；MySQL、Redis 等中间件配置默认指向 `192.168.101.130`
- 部署：后端使用 `script/start-backend-services.sh`，前端使用 `script/start-ui.sh`

## 3. 工程结构

```text
SemiRisk/
├── pom.xml
├── mvnw
├── semirisk-common/
│   ├── semirisk-common-core/
│   ├── semirisk-common-db/
│   ├── semirisk-common-middleware/
│   └── semirisk-common-ai/
├── semirisk-services/
│   ├── semirisk-gateway/
│   ├── semirisk-data-service/
│   ├── semirisk-risk-service/
│   ├── semirisk-ai-service/
│   ├── semirisk-alert-service/
│   └── semirisk-report-service/
├── semirisk-ui/
├── compose/
├── script/
└── doc/
```

## 4. 架构设计

系统当前采用前后端分离 + 微服务模块架构：

- `semirisk-ui`：Vue 前端，统一访问 Gateway API
- `semirisk-gateway`：认证、页面 API 聚合、兼容入口、CORS
- `semirisk-data-service`：公开 RSS 爬虫、每日数据刷新、上传/ETL 扩展边界
- `semirisk-risk-service`：每 24 小时自动风险测算、风险评分 API
- `semirisk-ai-service`：AI API Key 管理、AI 报告分析占位、模型调用扩展
- `semirisk-alert-service`：告警查询和状态流转边界，优先面向 MySQL
- `semirisk-report-service`：报告异步任务边界，支持 PDF、DOCX、PPTX 文件流
- `semirisk-common-core`：公共响应结构、服务端口、角色权限策略
- `semirisk-common-db`：预编译 SQL 模板和数据库共享约束
- `semirisk-common-middleware`：虚拟机中间件默认地址与端口
- `semirisk-common-ai`：AI 默认模型、Endpoint、环境变量名

## 5. 中间件设计

所有中间件统一使用虚拟机 IP `192.168.101.130`：

- MySQL：业务数据持久化
- Redis：登录失败计数、Token、验证码、短期任务状态
- Elasticsearch：RAG Hybrid Search
- MinIO：上传文件、PDF、报告文件存储
- RabbitMQ：ETL、报告生成和 Agent 异步任务
- Nacos：后续微服务注册配置中心

当前版本为保证本机可立即运行，账号、公开源告警状态、报告进度仍保留内存状态；风险页面不再回退固定演示风险，公开源不可达时显示待采集。Gateway 核心持久化接口采用数据库优先策略。Flyway 迁移脚本位于 `semirisk-services/semirisk-gateway/src/main/resources/db/migration`，连接 VM MySQL 后可通过 `SEMIRISK_FLYWAY_ENABLED=true` 自动迁移。

## 6. 关键业务流

### 6.1 登录与防爆破

用户提交账号、密码、验证码标识。后端在 5 分钟窗口内统计失败次数，达到 5 次锁定 30 分钟。登录成功后写入 Session 并返回前端 Token。
Redis 可用时失败计数和锁定状态写入 Redis；Redis 不可用时回退到内存计数。

### 6.2 数据上传清洗

前端支持点击、拖拽文件上传。后端限制 50MB，创建上传任务；用户点击解析后任务进入“导入成功”状态。SSE 接口实时推送 AI 清洗日志。

### 6.3 爬虫与每日刷新

`semirisk-data-service` 每天 00:00 拉取公开 RSS 源，抽取文章标题、原文链接、发布时间、风险维度和规则评分。公开源不可达时仅返回采集失败状态，不伪造风险事件。Gateway 聚合公开源记录为 Dashboard、告警、GIS、知识库、风险分析数据；`semirisk-risk-service` 每天 00:10 基于成功采集记录自动计算本日风险评分。

### 6.4 告警处理

预警中心从 `/api/alerts` 拉取公开源派生告警，支持关键词、等级、状态组合筛选。公开源告警包含原文链接。忽略告警会写入 Gateway 状态集，默认列表和计数器不再重复显示已忽略告警；批量处理只处理用户勾选的告警并流转为“处理中”。

### 6.5 报告生成

用户选择模板、语言、格式后创建异步报告任务。前端轮询任务进度，完成后根据任务格式下载 PDF、DOCX 或 PPTX 文件。文件生成逻辑位于 `semirisk-common-core`，Gateway 和 report-service 复用。

### 6.6 知识库 AI 智能体

知识库页面提供 RAG 问答。前端调用 `/api/knowledge/ask`，Gateway 先同步公开源爬虫记录，再按问题检索知识库条目，生成回答、检索链路和引用原文。未配置 DeepSeek API Key 时使用本地 RAG 摘要；配置后可在该接口扩展真实模型调用。

### 6.7 系统管理

系统管理页支持用户新增、启停、模型 Ping、Agent 触发、数据源重连等交互，均绑定后端 API。
系统管理页提供 AI 模型 Endpoint 与 API Key 配置入口，默认模型为 `deepseekv4-pro`，默认 Endpoint 为 `https://api.deepseek.com/v1`，Key 通过 `SEMIRISK_AI_API_KEY` 或页面配置注入，并在后端脱敏保存。

### 6.8 前端工程拆分

Vue 前端使用 Vue Router + Pinia。视图组件位于 `src/views`，业务动作位于 `src/composables`，接口封装位于 `src/api`。路由守卫根据登录态和模块权限控制页面访问。

### 6.9 运行治理

Gateway 对每个请求写入 `X-Trace-Id`，日志格式包含 TraceId。接口文档通过 OpenAPI 暴露，默认地址为 `/swagger-ui.html`。

## 7. Git 分支规范

- `main`：稳定版本
- `develop`：集成分支
- `feature/maven-bootstrap`：Maven 与 Spring Boot 初始化
- `feature/static-pages`：目标页面复用与前端交互
- `feature/core-api`：业务 API 与内存状态服务
- `feature/docs`：文档、部署、数据库脚本

Commit 使用 `type(scope): subject`，例如 `feat(api): add alert workflow endpoints`。

## 8. 启动与验证

```bash
./mvnw test
./script/start-backend-services.sh
./script/start-ui.sh
```

访问 `http://localhost:5173`，默认账号 `admin/password`。
