# SemiRisk 项目自审报告

## 1. 自审结论

当前项目可本地运行，前端 Vue、后端 Maven 多模块、公开源爬虫、认证权限、报告导出、预警处理、GIS 多线路和知识库问答链路已经打通。风险类页面已从固定演示数据改为公开 RSS 采集数据驱动；公开源不可达时显示待采集或采集失败。

## 2. 前端审查

- 结构：`src/api`、`src/composables`、`src/views`、`src/layout` 已拆分，页面不再集中在单文件逻辑中。
- 登录权限：未登录仅开放首页，侧栏和顶部均提供登录/注册入口。
- 注册：前端校验 QQ 邮箱，后端二次校验。
- GIS：已改为后端 `points` + `routes` 数据驱动，默认开启物流路径图层，避免固定一条或少量假线路。
- 预警中心：已补状态筛选、复选框批量处理、公开源链接、已忽略过滤。
- 知识库：已新增 AI 智能体问答面板，展示答案、检索链路和引用。

## 3. 后端审查

- 认证：登录失败计数优先走 Redis，Redis 不可用时内存兜底。
- 爬虫：`semirisk-data-service` 使用公开 RSS 源并返回原文 URL、维度、评分、采集状态。
- Gateway：聚合公开源数据到 Dashboard、Alert、GIS、Knowledge、Risk Analysis。
- 告警：公开源告警状态写入 Gateway 状态集，忽略和批处理后可正确反映到默认列表和计数器。
- 报告：`ReportFileFactory` 统一生成 PDF、DOCX、PPTX，Gateway 和 report-service 复用。
- AI 问答：`/api/knowledge/ask` 已实现本地 RAG 摘要和引用返回；DeepSeek Key 配置后可继续扩展真实模型调用。

## 4. API 审查

- API 映射文档已补充 `/api/knowledge/ask`、GIS `routes`、报告格式下载、告警状态规则。
- 数据库查询仍通过 `JdbcTemplate` 预编译参数执行。
- OpenAPI 入口保留：`/swagger-ui.html`、`/v3/api-docs`。

## 5. 文档审查

- `项目设计文档.md` 已更新公开源数据、报告格式、知识库智能体和告警状态。
- `PRD原型文档.md` 已更新 GIS 多线路、AI 问答、报告导出和告警交互验收。
- `API映射文档.md` 已更新新增接口和字段语义。
- `使用说明.md` 已更新公开源刷新、AI 问答和部署说明。

## 6. 中间件审查

- 中间件默认地址仍为 `192.168.101.130`。
- 本地可通过 SSH 隧道连接 VM 中间件，也可无中间件运行核心演示链路。
- MySQL、Redis、MinIO、Elasticsearch、RabbitMQ、Nacos 的部署脚本仍在 `script/` 和 `compose/` 下。
- Flyway 脚本保留在 Gateway 资源目录，生产建议开启 `SEMIRISK_FLYWAY_ENABLED=true`。

## 7. 剩余风险

- 公开 RSS 源不是专用商业数据源，文章覆盖度和时效性取决于源站。
- 企业工商权威数据源尚未接入，企业画像不会伪造工商字段，只显示待接入。
- `/api/knowledge/ask` 当前是本地 RAG 摘要，尚未真正调用 DeepSeek；需要在安全注入 Key 后接入模型调用。
- 公开源告警状态当前为 Gateway 内存状态，生产应迁移到 MySQL 表。
- GIS 坐标为公开源区域映射，不等同实时物流 GPS，需要接入真实 GIS/Webhook 后替换。
